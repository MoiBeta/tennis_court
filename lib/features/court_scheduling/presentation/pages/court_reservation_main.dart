import 'package:flutter/material.dart';

class CourtReservationMain extends StatelessWidget {
  const CourtReservationMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
