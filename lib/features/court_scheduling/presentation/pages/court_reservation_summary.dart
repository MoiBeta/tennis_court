import 'package:flutter/material.dart';

class CourtReservationSummary extends StatelessWidget {
  const CourtReservationSummary({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
