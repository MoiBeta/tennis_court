import 'package:flutter/material.dart';

class CourtReservationsList extends StatelessWidget {
  const CourtReservationsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
